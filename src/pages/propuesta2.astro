---
// Importar CSS global con Tailwind
import "../styles/globals.css";
import strapi from "../utils/strapi";
import type {
  StrapiEntity,
  Hero as HeroType,
  Header as HeaderType,
  Service,
} from "../types/strapi";
import { Image } from "astro:assets";

// Importar imágenes de certificaciones ISO
import cert9001 from "../assets/images/certifications/9001Blue.png";
import cert14001 from "../assets/images/certifications/14001Green.png";
import cert45001 from "../assets/images/certifications/45001Red.png";

// Obtener contenido dinámico desde Strapi
let heroData: StrapiEntity<HeroType> | null = null;
let headerData: StrapiEntity<HeaderType> | null = null;
let services: StrapiEntity<Service>[] = [];
let error: string | null = null;

try {
  // Usar endpoint estándar con filtro para heroes activos
  console.log("🔍 Obteniendo heroes activos");
  const heroResponse = await strapi.getActiveHeroes();

  // Tomar el primer hero activo (más reciente)
  heroData = heroResponse.data?.[0] || null;

  if (heroData) {
    console.log("🦸 Hero activo encontrado:", heroData.title);
  } else {
    console.log("⚠️ No hay heroes activos, probando cualquier hero...");
    const allHeroesResponse = await strapi.getHeroes();
    heroData = allHeroesResponse.data?.[0] || null;
    if (heroData) {
      console.log("🦸 Usando hero más reciente:", heroData.title);
    }
  }

  // Obtener Header desde Strapi
  console.log("🔍 Obteniendo header activo");
  headerData = await strapi.getActiveHeader();
  if (headerData) {
    console.log("🏠 Header activo encontrado:", headerData.companyName);
  }

  // Obtener servicios desde Strapi
  console.log("🔍 Obteniendo servicios activos");
  const servicesResponse = await strapi.getServices({
    filters: { isActive: { $eq: true } },
    sort: ["order:asc"],
  });
  services = servicesResponse.data || [];

  // Limitar a 4 servicios para el diseño
  if (services.length > 4) {
    services = services.slice(0, 4);
  }

  if (services.length > 0) {
    console.log(`✅ ${services.length} servicios encontrados`);
  } else {
    console.log("⚠️ No hay servicios activos, usando fallback");
    // Fallback con servicios hardcodeados
    services = [
      {
        id: 1,
        documentId: "fallback-1",
        createdAt: "",
        updatedAt: "",
        publishedAt: "",
        title: "Construcción Residencial",
        description:
          "Diseñamos y construimos hogares personalizados que combinan funcionalidad, estilo y durabilidad.",
        svgIcon: `<svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2H5a2 2 0 00-2-2z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 21l8-8-8-8"/></svg>`,
        iconColor: "blue" as const,
        order: 1,
        isActive: true,
        featured: false,
      },
      {
        id: 2,
        documentId: "fallback-2",
        createdAt: "",
        updatedAt: "",
        publishedAt: "",
        title: "Construcción Comercial",
        description:
          "Espacios comerciales de alta calidad que impulsan el éxito de su negocio.",
        svgIcon: `<svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/></svg>`,
        iconColor: "blue" as const,
        order: 2,
        isActive: true,
        featured: false,
      },
      {
        id: 3,
        documentId: "fallback-3",
        createdAt: "",
        updatedAt: "",
        publishedAt: "",
        title: "Proyectos Industriales",
        description:
          "Desarrollamos instalaciones industriales robustas, asegurando eficiencia y durabilidad.",
        svgIcon: `<svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 3v2m6-2v2M9 19v2m6-2v2M5 9H3m2 6H3m18-6h-2m2 6h-2M7 19h10a2 2 0 002-2V7a2 2 0 00-2-2H7a2 2 0 00-2 2v10a2 2 0 002 2zM9 9h6v6H9V9z"/></svg>`,
        iconColor: "blue" as const,
        order: 3,
        isActive: true,
        featured: false,
      },
      {
        id: 4,
        documentId: "fallback-4",
        createdAt: "",
        updatedAt: "",
        publishedAt: "",
        title: "Renovación y Remodelación",
        description:
          "Transforme su espacio con actualizaciones modernas y mejoras funcionales.",
        svgIcon: `<svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"/></svg>`,
        iconColor: "blue" as const,
        order: 4,
        isActive: true,
        featured: false,
      },
    ] as any;
  }
} catch (e) {
  error = e instanceof Error ? e.message : "Error loading content";
  console.warn("⚠️ Content loading failed, using fallbacks:", error);
}
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <meta name="generator" content={Astro.generator} />
    <title>Propuesta 2 - Diseño Industrial | SJT Contratistas</title>

    <!-- Fuentes de Google Fonts - Industrial y Moderna -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=Roboto:wght@300;400;500;700&display=swap"
      rel="stylesheet"
    />

    <!-- CSS Variables y Estilos Base - Paleta Industrial -->
    <style>
      :root {
        /* Colores Primarios - Paleta Industrial Azul */
        --color-primary-blue: #4a90e2;
        --color-primary-blue-hover: #357abd;
        --color-primary-blue-light: #5b9bd5;
        --color-primary-blue-dark: #2c5aa0;

        /* Colores Secundarios */
        --color-blue-dark: #1e3a8a;
        --color-blue-darker: #1e40af;
        --color-text-primary: #2c3e50;
        --color-text-secondary: #64748b;
        --color-text-light: #6b7280;

        /* Fondos */
        --color-bg-primary: #ffffff;
        --color-bg-light: #f8f9fa;
        --color-bg-lighter: #f1f5f9;
        --color-bg-gray: #f3f4f6;

        /* Acentos */
        --color-success: #10b981;
        --color-success-light: #34d399;

        /* Sombras */
        --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
        --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1);
        --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1);
        --shadow-xl: 0 20px 25px -5px rgb(0 0 0 / 0.1);

        /* Tipografía */
        --font-primary:
          "Inter", -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
        --font-secondary:
          "Roboto", -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
      }

      /* CSS Base para smooth scroll */
      html {
        scroll-behavior: smooth;
        font-family: var(--font-primary);
      }

      /* Offset para scroll con header fijo */
      .scroll-mt-20 {
        scroll-margin-top: 5rem;
      }

      /* Mejorar la experiencia de scroll */
      @media (prefers-reduced-motion: reduce) {
        html {
          scroll-behavior: auto;
        }
      }

      /* Utilidades custom para el diseño industrial */
      .text-primary-blue {
        color: var(--color-primary-blue);
      }

      .bg-primary-blue {
        background-color: var(--color-primary-blue);
      }

      .bg-primary-blue:hover {
        background-color: var(--color-primary-blue-hover);
      }

      .border-primary-blue {
        border-color: var(--color-primary-blue);
      }

      .text-success {
        color: var(--color-success);
      }

      /* Efectos de hover industriales */
      .hover-lift {
        transition:
          transform 0.3s ease,
          box-shadow 0.3s ease;
      }

      .hover-lift:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-lg);
      }

      /* Overlay para hero */
      .hero-overlay {
        background: linear-gradient(
          135deg,
          rgba(30, 58, 138, 0.85) 0%,
          rgba(44, 62, 80, 0.75) 100%
        );
      }

      /* Gradiente azul para CTA */
      .bg-blue-gradient {
        background: linear-gradient(
          135deg,
          var(--color-primary-blue) 0%,
          var(--color-blue-dark) 100%
        );
      }
    </style>
  </head>
  <body class="min-h-screen bg-white font-primary">
    <!-- Header Industrial Limpio -->
    <header
      class="bg-white shadow-sm border-b border-gray-100 sticky top-0 z-50"
    >
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-18 py-4">
          <!-- Logo y nombre de empresa -->
          <div class="flex items-center space-x-4">
            {
              headerData?.logoFile?.url ? (
                <img
                  src={headerData.logoFile.url}
                  alt={headerData.logoAlt || headerData.companyName}
                  class="h-12 w-auto object-contain"
                />
              ) : (
                <div class="h-10 w-10 bg-primary-blue rounded-lg flex items-center justify-center shadow-md">
                  <svg
                    class="h-6 w-6 text-white"
                    fill="currentColor"
                    viewBox="0 0 20 20"
                  >
                    <path d="M10.707 2.293a1 1 0 00-1.414 0l-9 9a1 1 0 001.414 1.414L10 4.414l8.293 8.293a1 1 0 001.414-1.414l-9-9z" />
                    <path d="M13 7v6a4 4 0 01-4 4H5a4 4 0 01-4-4V7a1 1 0 012 0v6a2 2 0 002 2h4a2 2 0 002-2V7a1 1 0 112 0z" />
                  </svg>
                </div>
              )
            }
            <div class="hidden sm:block">
              <h1
                class="text-xl font-bold"
                style="color: var(--color-text-primary);"
              >
                {headerData?.companyName || "SJT Contratistas"}
              </h1>
              <p class="text-sm text-primary-blue font-medium">
                Ingeniería & Construcción
              </p>
            </div>
          </div>

          <!-- Navegación central -->
          <nav class="hidden lg:flex space-x-8">
            <a
              href="/"
              class="text-gray-600 hover:text-primary-blue transition-colors duration-200 font-medium px-3 py-2 rounded-md hover:bg-blue-50"
              >Inicio</a
            >
            <a
              href="#"
              class="text-gray-600 hover:text-primary-blue transition-colors duration-200 font-medium px-3 py-2 rounded-md hover:bg-blue-50"
              >Servicios</a
            >
            <a
              href="#"
              class="text-gray-600 hover:text-primary-blue transition-colors duration-200 font-medium px-3 py-2 rounded-md hover:bg-blue-50"
              >Proyectos</a
            >
            <a
              href="#"
              class="text-gray-600 hover:text-primary-blue transition-colors duration-200 font-medium px-3 py-2 rounded-md hover:bg-blue-50"
              >Nosotros</a
            >
          </nav>

          <!-- Información de contacto y botón -->
          <div class="flex items-center space-x-6">
            {
              headerData?.phoneNumber && (
                <div class="hidden xl:flex items-center space-x-2 text-gray-600">
                  <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 20 20">
                    <path d="M2 3a1 1 0 011-1h2.153a1 1 0 01.986.836l.74 4.435a1 1 0 01-.54 1.06l-1.548.773a11.037 11.037 0 006.105 6.105l.774-1.548a1 1 0 011.059-.54l4.435.74a1 1 0 01.836.986V17a1 1 0 01-1 1h-2C7.82 18 2 12.18 2 5V3z" />
                  </svg>
                  <span class="text-sm font-medium">
                    {headerData.phoneNumber}
                  </span>
                </div>
              )
            }
            <a
              href="#"
              class="bg-primary-blue hover:bg-primary-blue-hover text-white px-5 py-2.5 rounded-lg font-semibold text-sm transition-all duration-200 shadow-md hover:shadow-lg hover-lift"
            >
              {headerData?.buttonText || "CONTACTANOS"}
            </a>
          </div>
        </div>
      </div>
    </header>

    <!-- Hero Section Fullscreen con Overlay Industrial -->
    <section
      class="relative min-h-screen flex items-center justify-center overflow-hidden"
    >
      <!-- Imagen de fondo industrial -->
      <div class="absolute inset-0 z-0">
        <img
          src={heroData?.heroImage?.url ||
            "https://images.unsplash.com/photo-1581094794329-c8112a89af12?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80"}
          alt={heroData?.heroImage?.alternativeText ||
            "Instalaciones industriales modernas"}
          class="w-full h-full object-cover"
        />
        <!-- Overlay con gradiente industrial -->
        <div class="absolute inset-0 hero-overlay"></div>
      </div>

      <!-- Contenido centrado sobre la imagen -->
      <div
        class="relative z-10 text-center px-4 sm:px-6 lg:px-8 max-w-4xl mx-auto"
      >
        <div class="space-y-8">
          <!-- Badge superior -->
          <div
            class="inline-flex items-center px-4 py-2 bg-white/10 backdrop-blur-sm border border-white/20 rounded-full"
          >
            <span class="text-white text-sm font-medium">
              🏗️ Líder en Construcción Industrial
            </span>
          </div>

          <!-- Título principal -->
          <h1
            class="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-bold text-white leading-tight"
          >
            {
              heroData?.title ||
                "Su Socio Confiable en Construcción e Infraestructura"
            }
          </h1>

          <!-- Descripción -->
          <p
            class="text-xl sm:text-2xl text-blue-100 leading-relaxed max-w-3xl mx-auto font-light"
          >
            {
              heroData?.subtitle ||
                "Expertos de confianza en soluciones especializadas de construcción para proyectos residenciales, comerciales, industriales y de infraestructura"
            }
          </p>

          <!-- Botones de acción -->
          <div
            class="flex flex-col sm:flex-row gap-4 justify-center items-center pt-8"
          >
            <a
              href="#mission-section"
              class="bg-primary-blue hover:bg-primary-blue-hover text-white px-8 py-4 rounded-lg font-semibold text-lg transition-all duration-300 hover-lift shadow-xl inline-flex items-center space-x-2"
            >
              <span>{heroData?.buttonText || "Ver Nuestros Servicios"}</span>
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M19 9l-7 7-7-7"></path>
              </svg>
            </a>

            <a
              href="#"
              class="border border-white text-white hover:bg-white hover:text-blue-900 px-8 py-4 rounded-lg font-semibold text-lg transition-all duration-300 hover-lift backdrop-blur-sm"
            >
              Contactanos
            </a>
          </div>
        </div>
      </div>

      <!-- Indicador de scroll -->
      <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 z-10">
        <div class="animate-bounce">
          <svg
            class="w-6 h-6 text-white/70"
            fill="none"
            stroke="currentColor"
            viewBox="0 0 24 24"
          >
            <path
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
          </svg>
        </div>
      </div>
    </section>

    <!-- Tira de Certificaciones ISO - American Total Quality -->
    <section class="bg-white py-8 border-t-4 border-primary-blue shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div
          class="flex flex-col lg:flex-row items-center justify-center space-y-6 lg:space-y-0 lg:space-x-12"
        >
          <!-- Texto principal -->
          <div class="text-center lg:text-left">
            <h3
              class="text-2xl lg:text-3xl font-black text-gray-800 tracking-wide"
            >
              AMERICAN TOTAL QUALITY
            </h3>
            <p
              class="text-sm text-gray-600 font-semibold uppercase tracking-wider mt-1"
            >
              Certificaciones Internacionales
            </p>
          </div>

          <!-- Separador vertical -->
          <div class="hidden lg:block w-px h-16 bg-gray-300"></div>

          <!-- Grid de Certificaciones ISO -->
          <div class="flex items-center justify-center space-x-8">
            <!-- ISO 9001 -->
            <div
              class="group transition-all duration-300 hover:-translate-y-2 hover:shadow-xl"
            >
              <Image
                src={cert9001}
                alt="ISO 9001 - Sistema de Gestión de Calidad"
                width={80}
                height={80}
                class="h-20 w-20 object-contain transition-transform duration-300 group-hover:scale-110"
                loading="lazy"
              />
            </div>

            <!-- ISO 14001 -->
            <div
              class="group transition-all duration-300 hover:-translate-y-2 hover:shadow-xl"
            >
              <Image
                src={cert14001}
                alt="ISO 14001 - Sistema de Gestión Ambiental"
                width={80}
                height={80}
                class="h-20 w-20 object-contain transition-transform duration-300 group-hover:scale-110"
                loading="lazy"
              />
            </div>

            <!-- ISO 45001 -->
            <div
              class="group transition-all duration-300 hover:-translate-y-2 hover:shadow-xl"
            >
              <Image
                src={cert45001}
                alt="ISO 45001 - Sistema de Gestión de Seguridad y Salud Ocupacional"
                width={80}
                height={80}
                class="h-20 w-20 object-contain transition-transform duration-300 group-hover:scale-110"
                loading="lazy"
              />
            </div>
          </div>

          <!-- Badge de calidad -->
          <div class="hidden lg:flex items-center space-x-3">
            <div class="w-2 h-2 rounded-full bg-success animate-pulse"></div>
            <span
              class="text-xs text-gray-500 font-medium uppercase tracking-wider"
            >
              Certified
            </span>
          </div>
        </div>

        <!-- Línea decorativa inferior -->
        <div class="mt-6 flex justify-center">
          <div
            class="w-24 h-1 bg-gradient-to-r from-primary-blue to-blue-dark rounded-full"
          >
          </div>
        </div>
      </div>
    </section>

    <!-- Values Section - Imagen + Checkmarks Verdes (Formato Industrial) -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
          <!-- Imagen profesional lado izquierdo -->
          <div class="relative">
            <img
              src="https://images.unsplash.com/photo-1504307651254-35680f356dfd?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80"
              alt="Profesional trabajando en laboratorio industrial"
              class="rounded-2xl shadow-xl w-full h-96 object-cover"
            />
            <!-- Badge floating -->
            <div
              class="absolute top-6 left-6 bg-white/90 backdrop-blur-sm px-4 py-2 rounded-full shadow-lg"
            >
              <span class="text-sm font-semibold text-primary-blue">
                🏗️ Más de XX años de experiencia
              </span>
            </div>
          </div>

          <!-- Contenido lado derecho -->
          <div class="space-y-8">
            <div>
              <div
                class="inline-flex items-center px-4 py-2 bg-green-100 text-green-800 rounded-full text-sm font-medium mb-6"
              >
                Nuestros Valores
              </div>
              <h2
                class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight mb-6"
              >
                Forjando el futuro de la construcción mediante la entrega de
                espacios innovadores
              </h2>
              <p class="text-xl text-gray-600 leading-relaxed">
                Con más de XX años de experiencia, SJT ha sido líder en la
                industria de la construcción, entregando proyectos residenciales
                y comerciales de alta calidad.
              </p>
            </div>

            <!-- Lista de valores con checkmarks verdes -->
            <div class="space-y-4">
              {
                [
                  "Compromiso con Nuestros Colaboradores",
                  "Innovación en Nuestros Procesos",
                  "Confianza y Credibilidad de Nuestros Clientes",
                  "Excelencia en el Trabajo",
                  "Transparencia en Nuestras Relaciones",
                  "Integridad y Rectitud en Nuestro Actuar",
                  "Respeto por la Comunidad",
                ].map((valor) => (
                  <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0 mt-1">
                      <svg
                        class="w-6 h-6 text-success"
                        fill="currentColor"
                        viewBox="0 0 20 20"
                      >
                        <path
                          fill-rule="evenodd"
                          d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                          clip-rule="evenodd"
                        />
                      </svg>
                    </div>
                    <h3 class="text-lg font-semibold text-gray-900 leading-relaxed">
                      {valor}
                    </h3>
                  </div>
                ))
              }
            </div>

            <!-- Botón de acción -->
            <div class="pt-6">
              <a
                href="#"
                class="inline-flex items-center bg-primary-blue hover:bg-primary-blue-hover text-white px-6 py-3 rounded-lg font-semibold transition-all duration-300 hover-lift shadow-md space-x-2"
              >
                <span>Conoce más sobre nosotros</span>
                <svg
                  class="w-4 h-4"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                </svg>
              </a>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Mission Section - Elementos Visuales Modernos -->
    <section id="mission-section" class="py-20 bg-gray-50 scroll-mt-20">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <!-- Encabezado de sección -->
        <div class="text-center mb-16">
          <div
            class="inline-flex items-center px-4 py-2 bg-primary-blue/10 text-primary-blue rounded-full text-sm font-medium mb-4"
          >
            Nuestra Misión
          </div>
          <h2
            class="text-4xl md:text-5xl font-bold text-gray-900 mb-6 leading-tight"
          >
            Soluciones Integrales de Construcción
          </h2>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
            Desde proyectos residenciales hasta desarrollos comerciales e
            industriales, ofrecemos todo lo que necesita para optimizar sus
            procesos y lograr resultados excepcionales.
          </p>
        </div>

        <!-- Mission Statement con diseño moderno -->
        <div
          class="bg-white rounded-3xl p-8 md:p-12 shadow-lg border border-gray-100"
        >
          <div class="grid lg:grid-cols-2 gap-12 items-center">
            <!-- Contenido de misión -->
            <div class="space-y-6">
              <div>
                <div
                  class="inline-flex items-center px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-6"
                >
                  Nuestra Misión
                </div>
                <h3
                  class="text-3xl md:text-4xl font-bold text-gray-900 leading-tight mb-6"
                >
                  Calidad, Integridad, Resultados.
                </h3>
              </div>
              <div class="text-lg text-gray-700 leading-relaxed space-y-4">
                <p>
                  <strong class="text-primary-blue"
                    >Ofrecer a nuestros clientes productos y servicios de
                    ingeniería de excelente calidad</strong
                  > en los tiempos establecidos, mediante una gestión íntegra y honesta.
                </p>
                <p>
                  Ofreciendo estabilidad laboral y oportunidades de desarrollo
                  al personal que labora en la empresa, <strong
                    class="text-primary-blue"
                    >asegurando la inversión de los clientes y excediendo sus
                    expectativas</strong
                  >.
                </p>
                <p>
                  A fin de garantizar la rentabilidad de las operaciones y el
                  cumplimiento de los compromisos con proveedores.
                </p>
              </div>
            </div>

            <!-- Elementos visuales -->
            <div class="space-y-6">
              <div class="grid grid-cols-2 gap-4">
                <div class="bg-blue-50 rounded-xl p-6 text-center">
                  <div class="text-3xl font-bold text-primary-blue mb-2">
                    XX+
                  </div>
                  <p class="text-sm text-gray-600">Años de experiencia</p>
                </div>
                <div class="bg-green-50 rounded-xl p-6 text-center">
                  <div class="text-3xl font-bold text-success mb-2">500+</div>
                  <p class="text-sm text-gray-600">Proyectos completados</p>
                </div>
                <div class="bg-purple-50 rounded-xl p-6 text-center">
                  <div class="text-3xl font-bold text-purple-600 mb-2">
                    100%
                  </div>
                  <p class="text-sm text-gray-600">Satisfacción cliente</p>
                </div>
                <div class="bg-orange-50 rounded-xl p-6 text-center">
                  <div class="text-3xl font-bold text-orange-600 mb-2">
                    24/7
                  </div>
                  <p class="text-sm text-gray-600">Soporte técnico</p>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Vision Section - Imagen + Lista Professional -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
          <!-- Contenido de visión lado izquierdo -->
          <div class="space-y-8">
            <div>
              <div
                class="inline-flex items-center px-4 py-2 bg-blue-100 text-blue-800 rounded-full text-sm font-medium mb-6"
              >
                Innovación y Desarrollo
              </div>
              <h2
                class="text-4xl md:text-5xl font-bold text-gray-900 leading-tight mb-6"
              >
                Avanzando en la Construcción a Través de la Innovación
              </h2>
              <p class="text-xl text-gray-600 leading-relaxed">
                La innovación está profundamente arraigada en nuestro ADN.
                Nuestras instalaciones combinan experiencia técnica
                especializada con conocimiento práctico de construcción para
                crear soluciones que definen los estándares de la industria.
              </p>
            </div>

            <!-- Lista de puntos de visión con checkmarks -->
            <div class="space-y-4">
              {
                [
                  "Mejora continua en la eficiencia de procesos y rendimiento",
                  "Desarrollo de métodos de construcción eco-amigables",
                  "Equipos multifuncionales dedicados a la innovación de proyectos",
                  "Alianzas estratégicas con líderes tecnológicos globales",
                ].map((point) => (
                  <div class="flex items-start space-x-4">
                    <div class="flex-shrink-0 mt-1">
                      <svg
                        class="w-6 h-6 text-success"
                        fill="currentColor"
                        viewBox="0 0 20 20"
                      >
                        <path
                          fill-rule="evenodd"
                          d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z"
                          clip-rule="evenodd"
                        />
                      </svg>
                    </div>
                    <p class="text-lg text-gray-700 leading-relaxed">{point}</p>
                  </div>
                ))
              }
            </div>

            <!-- Contenido de visión SJT -->
            <div class="bg-blue-50 rounded-2xl p-8">
              <h3 class="text-2xl font-bold text-gray-900 mb-4">
                Nuestra Visión SJT
              </h3>
              <p class="text-lg text-gray-700 leading-relaxed">
                <strong class="text-primary-blue"
                  >Ser reconocida como una empresa líder y dinámica</strong
                >, comprometidos a romper esquemas, colocándonos a la vanguardia
                en todos y cada uno de nuestros procesos para nuestros
                proyectos, productos y servicios.
              </p>
            </div>
          </div>

          <!-- Imagen profesional/industrial lado derecho -->
          <div class="relative">
            <img
              src="https://images.unsplash.com/photo-1582719471384-894fbb16e074?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80"
              alt="Investigación y desarrollo en laboratorio industrial"
              class="rounded-2xl shadow-xl w-full h-96 object-cover"
            />
            <!-- Stats overlay -->
            <div class="absolute bottom-6 left-6 right-6">
              <div
                class="bg-white/90 backdrop-blur-sm rounded-xl p-4 shadow-lg"
              >
                <div class="text-center">
                  <p class="text-sm text-gray-600 mb-2">
                    Algunas empresas para las que hemos brindado soluciones
                  </p>
                  <div class="flex justify-center items-center space-x-4">
                    <div class="w-8 h-8 bg-gray-200 rounded"></div>
                    <div class="w-8 h-8 bg-gray-200 rounded"></div>
                    <div class="w-8 h-8 bg-gray-200 rounded"></div>
                    <span class="text-sm text-gray-500">+50 más</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA Section - Azul Full-Width (Nueva Sección) -->
    <section class="py-20 bg-blue-gradient relative overflow-hidden">
      <!-- Patrón de fondo decorativo -->
      <div class="absolute inset-0 opacity-10">
        <svg
          class="w-full h-full"
          viewBox="0 0 100 100"
          xmlns="http://www.w3.org/2000/svg"
        >
          <defs>
            <pattern
              id="grid"
              width="10"
              height="10"
              patternUnits="userSpaceOnUse"
            >
              <path
                d="M 10 0 L 0 0 0 10"
                fill="none"
                stroke="white"
                stroke-width="0.5"></path>
            </pattern>
          </defs>
          <rect width="100" height="100" fill="url(#grid)"></rect>
        </svg>
      </div>

      <div class="max-w-4xl mx-auto text-center px-4 sm:px-6 lg:px-8 relative">
        <div class="space-y-8">
          <!-- Encabezado -->
          <div>
            <h2
              class="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight mb-6"
            >
              Escalamos Tus<br />Proyectos de Construcción
            </h2>
            <p
              class="text-xl md:text-2xl text-blue-100 leading-relaxed max-w-3xl mx-auto"
            >
              Descubre cómo nuestras soluciones comprobadas pueden ayudarte a
              lograr calidad consistente y mejores resultados.
            </p>
          </div>

          <!-- Botones de acción -->
          <div
            class="flex flex-col sm:flex-row gap-6 justify-center items-center pt-8"
          >
            <a
              href="#"
              class="bg-white text-primary-blue hover:bg-gray-50 px-8 py-4 rounded-lg font-bold text-lg transition-all duration-300 hover-lift shadow-xl inline-flex items-center space-x-2"
            >
              <span>Contáctanos Hoy</span>
              <svg
                class="w-5 h-5"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
              </svg>
            </a>

            <a
              href="#"
              class="border-2 border-white text-white hover:bg-white hover:text-primary-blue px-8 py-4 rounded-lg font-semibold text-lg transition-all duration-300 hover-lift backdrop-blur-sm"
            >
              Ver Proyectos
            </a>
          </div>

          <!-- Estadísticas rápidas -->
          <div class="grid grid-cols-2 md:grid-cols-4 gap-8 pt-16">
            {
              [
                { number: "XX+", label: "Años de experiencia" },
                { number: "500+", label: "Proyectos completados" },
                { number: "50+", label: "Clientes satisfechos" },
                { number: "24/7", label: "Soporte técnico" },
              ].map((stat) => (
                <div class="text-center">
                  <div class="text-3xl md:text-4xl font-bold text-white mb-2">
                    {stat.number}
                  </div>
                  <p class="text-blue-100 text-sm">{stat.label}</p>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <!-- Botón flotante para regresar -->
    <div class="fixed bottom-6 right-6 z-50">
      <a
        href="#"
        class="bg-gray-900 hover:bg-gray-800 text-white px-4 py-3 rounded-full font-semibold transition-all duration-300 shadow-lg hover-lift flex items-center space-x-2"
      >
        <svg
          class="w-4 h-4"
          fill="none"
          stroke="currentColor"
          viewBox="0 0 24 24"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
        </svg>
        <span class="hidden sm:inline">Inicio</span>
      </a>
    </div>

    <!-- JavaScript para interacciones y smooth scroll -->
    <script>
      document.addEventListener("DOMContentLoaded", function () {
        console.log("🎨 Propuesta 2 - Diseño Industrial completado");
        console.log("📊 Variables CSS industriales cargadas");

        // Smooth scroll para enlaces anchor
        const smoothScrollLinks = document.querySelectorAll('a[href^="#"]');

        smoothScrollLinks.forEach((link) => {
          link.addEventListener("click", (e: Event) => {
            e.preventDefault();

            const target = e.currentTarget as HTMLAnchorElement;
            const targetId = target.getAttribute("href");
            if (!targetId) return;

            const targetSection = document.querySelector(targetId);

            if (targetSection) {
              targetSection.scrollIntoView({
                behavior: "smooth",
                block: "start",
              });

              // Actualizar URL sin hacer jump
              if (targetId) {
                history.replaceState({}, "", targetId);
              }
            }
          });
        });

        // Animaciones al hacer scroll (Intersection Observer)
        const observerOptions = {
          threshold: 0.1,
          rootMargin: "0px 0px -50px 0px",
        };

        const observer = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              (entry.target as HTMLElement).style.opacity = "1";
              (entry.target as HTMLElement).style.transform = "translateY(0)";
            }
          });
        }, observerOptions);

        // Observar secciones para animaciones
        const sections = document.querySelectorAll("section");
        sections.forEach((section) => {
          const sectionElement = section as HTMLElement;
          sectionElement.style.opacity = "0";
          sectionElement.style.transform = "translateY(20px)";
          sectionElement.style.transition =
            "opacity 0.6s ease, transform 0.6s ease";
          observer.observe(section);
        });

        // Hero section siempre visible
        const heroSection = document.querySelector("section");
        if (heroSection) {
          const heroElement = heroSection as HTMLElement;
          heroElement.style.opacity = "1";
          heroElement.style.transform = "translateY(0)";
        }

        // Performance logging
        console.log("✅ Todas las secciones implementadas:");
        console.log("   - Header Industrial");
        console.log("   - Hero Fullscreen + Overlay");
        console.log("   - Services (Cards blancas + hover elevation)");
        console.log("   - Values (Imagen + checkmarks verdes)");
        console.log("   - Mission (Elementos visuales modernos)");
        console.log("   - Vision (Imagen profesional + lista)");
        console.log("   - Benefits (4 columnas + íconos)");
        console.log("   - CTA Azul (Full-width)");
        console.log("🚀 Propuesta 2 - Diseño Industrial COMPLETADA");
      });
    </script>
  </body>
</html>
